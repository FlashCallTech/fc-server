import React from "react";

const NotAuthorized = () => {
	return (
		<section className="size-full h-screen flex flex-col gap-2 items-center justify-center  text-2xl font-semibold text-center text-red-500">
			Unauthorized Access
		</section>
	);
};

export default NotAuthorized;
